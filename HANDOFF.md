# HANDOFF — לוח הוועד הדיגיטלי עפרוני 18

מסמך זה מסכם את כל מה שצריך לדעת אם פותחים שיחה חדשה. קרא אותו ראשון.

---

## 🎯 מה זה הפרויקט

**לוח שילוט דיגיטלי לבניין** "עפרוני 18, מודיעין" — תלוי בלובי שמול המעלית, מציג מידע לדיירים שמחכים למעלית או נכנסים/יוצאים מהבניין.

**המשתמש:** ח. כהן (הקופירייט בלוח: "© כל הזכויות שמורות לח. כהן").  
**השפה:** עברית RTL בלבד.  
**הקהל:** דיירי הבניין + אורחים + שומרי שבת.

---

## 📁 מיקום הקבצים

הכל ב-`C:\vaad-board-site\` (לא ב-OneDrive — בדרך שהיא קצרה ו-clean ל-Git):

```
C:\vaad-board-site\
├── index.html              ← הקובץ הראשי. כל הלוגיקה, ה-CSS, ה-HTML, וה-data במקום אחד.
├── kiosk.bat               ← מפעיל Chrome --kiosk לסטרימר.
├── test-local.bat          ← מריץ http-server ב-localhost:3000 לבדיקה.
├── README.txt              ← הוראות התקנה לסטרימר (בעברית).
├── HANDOFF.md              ← הקובץ הזה.
├── preview-shabbat.html    ← תצוגה מקדימה של פאנל השבת (לא בשימוש בלוח החי).
├── preview-weather.html    ← תצוגה מקדימה של מזג אוויר.
├── preview-clock-center.html ← השוואת מרכוז שעון.
└── music/
    └── HotelLobby_BoutiqueJazz_2h.mp3  ← מוזיקת רקע (78MB דחוס).
```

---

## 🌐 GitHub & פריסה

| | |
|---|---|
| **Repo** | https://github.com/cochag12/vaad-hefroni- |
| **Live URL** | https://cochag12.github.io/vaad-hefroni-/ |
| **Branch** | `main` |
| **Pages** | GitHub Pages מסונכרן אוטומטית עם `main` |
| **Working repo (local)** | `%TEMP%\vaad-push` (קלון ל-push) |

**זרימת push** (תמיד מהקלון, לא מהפרויקט המקורי):
```powershell
$work = "$env:TEMP\vaad-push"; Set-Location $work
git pull --rebase origin main
Copy-Item 'C:\vaad-board-site\index.html' "$work\index.html" -Force
git add index.html
git commit -m "..."
git push origin main
```

ה-Git credentials של `cochag12` שמורים ב-Windows Credential Manager. אין צורך בהזדהות ידנית.

---

## 🖥 הסטרימר

**מה זה:** מחשב קטן (PC Stick / מיני PC) שמחובר לטלוויזיה בלובי הבניין דרך HDMI, מריץ Windows + Chrome ב-`--kiosk` מצב.

**הפעלה:** `kiosk.bat` מפעיל Chrome כך:
```bat
chrome --kiosk --autoplay-policy=no-user-gesture-required --noerrdialogs --disable-infobars --disable-session-crashed-bubble --user-data-dir="%TEMP%\vaad-kiosk" "https://cochag12.github.io/vaad-hefroni-/"
```

**הפלאג הקריטי:** `--autoplay-policy=no-user-gesture-required` — בלעדיו המוזיקה לא מתחילה לבד.

**מצב תמיד-פעיל:**
- Windows Login אוטומטי (netplwiz)
- Screen never sleeps (Power settings → Never)
- כל יום ב-04:00 הדף עושה `location.reload()` (קלינין זיכרון + קליטת קוד חדש)

---

## 🏗 ארכיטקטורה (כלל זהב)

**הכל הוא קובץ HTML אחד סטטי**. אין שרת. אין מסד נתונים. אין React. אין build.

- **CDN בלבד:**
  - Tailwind CSS via Play CDN (script tag)
  - Heebo font from Google Fonts
- **APIs חיצוניים (CORS-friendly):**
  - Open-Meteo (מזג אוויר + זריחה/שקיעה) — חינמי, ללא מפתח
  - Hebcal (זמני שבת + פרשה) — חינמי, ללא מפתח
  - HE Wikipedia (תוכן דמות) — חינמי, ללא מפתח
  - rss2json (חדשות RSS דרך CORS proxy) — חינמי, ללא מפתח
  - Tzevaadom WebSocket (התראות צבע אדום) — חינמי, ציבורי
- **מוזיקה:** MP3 מקומי בתיקייה (אפס צריכת אינטרנט)

**עקרון מנחה:** אם אפשר לעשות משהו ב-vanilla JS + CSS, לא להוסיף תלות. בקבועיים שמרניים. **אסור לעבור ל-Firebase או React Native** — נדון, נדחה, יקר.

---

## 🧱 מבנה הדף (פאנלים)

```
┌────────────────────────────────────────────────────────────────────┐
│ Top Bar                                                            │
│ עפרוני 18 + טאגליין + ©         שעון+תאריך (מרכז מסך)         🎵 │
├──────────┬─────────────────────────────────────┬──────────────────┤
│ מזג אוויר │                                     │ כללי הבית המשותף │
│ זמני יום  │     פאנל מרכזי                     │  (40% גובה)     │
│           │     - דמות יומית (חול)              ├──────────────────┤
│           │     - הגל השקט + פרשה (שבת)         │ כותרות נבחרות   │
│           │                                     │  (60% גובה)     │
├──────────┴─────────────────────────────────────┴──────────────────┤
│ מבזקים (טיקר חדשות אופקי)                                          │
└────────────────────────────────────────────────────────────────────┘
```

---

## 🎨 עיצוב — קבועים חשובים

- **פלטה:** night-900 (`#020410`) רקע + brand-400 (`#22D3EE` ציאן) הדגשים + accent-600 (`#8B5CF6` סגול עדין)
- **גופן:** Heebo (Google Fonts) — RTL מובנה
- **סגנון:** Glassmorphism + ניאון/סייברפאנק עדין. **לא** "מלון בוטיק רגוע".
  - היה ניסיון בעבר להציע wood-and-gold — נדחה.
  - הסגנון הזה מתאים לבניין מודרני במודיעין.
- **Tailwind:** Play CDN. **שים לב:** ערכים שרירותיים כמו `lg:flex-[2]` **לא נתמכים**. השתמש ב-CSS מותאם תחת `<style>` עם media query.
  - דוגמא: `.rules-panel-grow { flex: 2 2 0% !important; }` מוגדר כ-CSS, ולא `lg:flex-[2]`.
- **Breakpoints מותאמים** (Tailwind config):
  ```js
  screens: { 'sm':'480px', 'md':'600px', 'lg':'700px', 'xl':'1024px', '2xl':'1280px' }
  ```
  ה-`lg` הורד ל-700px כדי שהפריסה הדסקטופית תופיע על הסטרימר גם ב-720p.

---

## 🔄 קצבי עדכון

| רכיב | קצב | מקור |
|------|------|------|
| שעון + תאריך | כל שנייה | פנימי (tickClock) |
| בדיקות שבת / יום כיפור | כל דקה | פנימי |
| מבזקים (טיקר תחתון) | כל 10 דקות | RSS (Ynet) |
| כותרות נבחרות (ימני) | כל 20 דקות | RSS (וואלה/ספורט/גלובס) |
| **החלפת כותרת בפאנל** | **כל 15 שניות** | פנימי (3 כותרות בעמוד) |
| **החלפת כלל בית** | **כל 21 שניות** | פנימי |
| מזג אוויר | כל 6 שעות | Open-Meteo |
| זמני זריחה/שקיעה | כל 6 שעות | Open-Meteo |
| זמני שבת + פרשה | כל 6 שעות | Hebcal |
| דמות יומית | בחצות + רענון יומי | Wikipedia |
| **רענון מלא של הדף** | **04:00 בלילה** | location.reload() |
| התראות צבע אדום | רציף (WebSocket) | Tzevaadom |

---

## 🚨 הגל השקט (Pikud HaOref alerts)

- **WebSocket** ל-`wss://ws.tzevaadom.co.il/socket?platform=WEB`
- **ערים מסוננות:** מודיעין-מכבים-רעות בלבד (קבוע `MY_CITIES`)
- **Reconnect:** exponential backoff (1s→2s→4s→8s→16s→32s→60s max)
- **Heartbeat:** ping כל 30 שניות
- **חשיפה גלובלית:** `window.vaadAlertConnected` + `window.vaadAlertStatus()`
- **בדיקה ידנית:** `window.vaadTestAlert()` בקונסולה → מציג אזעקה לדוגמה
- **מסך אזעקה:** רקע אדום פועם + צפר 45 שניות (Web Audio API)
- **מרחב מוגן:** קונסטנט `SHELTER_INFO` עם:
  - primary: "מרחב מוגן: קומה 1"
  - secondary: "עלו/רדו מיד למרחב המוגן"
  - warning: "התרחקו מחלונות וקירות חיצוניים"

---

## 🕯 שבת — הגל השקט + תקציר פרשה

החל מכניסת שבת ועד מוצאי שבת, **הפאנל המרכזי** (שבדרך כלל מציג את הדמות היומית) מתחלף ל:
```
שבת שלום
פרשת [שם]

📜 תקציר הפרשה
[תקציר מלא ~130 מילים]

כניסת שבת  צאת שבת
  19:24      20:25

🟢 הגל השקט · ניטור התרעות פעיל
```

- **תקצירי פרשה:** מערך `PARSHIYOT` עם **61 רשומות** (54 פרשיות + 7 פרשיות משולבות).
- **מפתח חיפוש:** שם הפרשה כפי שמחזיר Hebcal (למשל `"בהעלתך"`), בלי "פרשת ".
- **מוזיקה ממשיכה בשבת** (רק יום כיפור עוצר).

---

## 🎵 מוזיקה

- **קובץ יחיד:** `music/HotelLobby_BoutiqueJazz_2h.mp3` (~78MB, 84 דקות)
- **מקור:** Internet Archive — "Hotel Lobby Music 2020 Instrumental Jazz Lounge"
- **דחיסה:** הקובץ המקורי היה 193MB (320kbps), דחסנו ל-128kbps כדי לעמוד ב-100MB limit של GitHub.
- **נגן:** HTML5 `<audio>` בלולאה אינסופית
- **ברירת מחדל:** מנגן אוטומטית בעלייה (`--autoplay-policy` flag + JS fallback ל-`onFirstInteraction`)
- **כיבוי יזום:** המשתמש לוחץ על כפתור ⏸ → נשמר ב-localStorage `vaad-music-wants=0`
- **כיבוי מערכתי (יום כיפור):** עוצר אבל לא משנה את `vaad-music-wants` → כשיוצא מכיפור חוזר אוטומטית

**להוסיף מוזיקה נוספת:**
```js
var MUSIC = [
  {file:"music/HotelLobby_BoutiqueJazz_2h.mp3", name:"מלון בוטיק · ג׳אז לונג׳ (1:24)"},
  // הוסף כאן: {file:"music/X.mp3", name:"שם להצגה"},
];
```
אם יש >1 קובץ, ה-`<select>` יחזור להופיע אוטומטית.

---

## 📜 כללי הבית המשותף

**33 כללים** מבוסס על תקנון רשמי שהמשתמש שלח ("כללי התנהגות - עפרוני 18.pdf"). הוא משולב ב-`HOUSE_RULES` array.

- **הצגה:** כלל אחד בכל פעם, fade בין מעברים, **קצב 21 שניות**.
- **התאמת גופן דינמית:** אם כלל ארוך, מקטין הדרגתית מ-18px ל-11px עד שיכנס. אם גם 11 לא מספיק — נשאר 11.

**עריכת כלל מסוים:** פתח את `index.html`, מצא `HOUSE_RULES = [`, ערוך את הטקסט בין המרכאות. שמור, push.

---

## 📰 כותרות נבחרות

- **3 מקורות:** וואלה (אדום) + ספורט Ynet (ירוק) + גלובס (כתום)
- **תצוגה:** 3 כותרות בעמוד, מתחלפות **כל 15 שניות**
- **קיצוץ פרוגרסיבי:** מתחיל מ-110 תווים לכל כותרת, מקצץ הדרגתית עד שכל 3 הכותרות נכנסות (הסטרימר עם RTL Hebrew רנדר line-clamp לעיתים בעייתי, לכן JS truncate)

---

## 🌤 מזג אוויר

- **מקור:** Open-Meteo (חינמי, ללא מפתח)
- **מיקום:** מודיעין (קואורדינטות בקבוע `MODIIN`)
- **מציג:**
  - טמפ׳ נוכחית גדולה + (כעת) קטן בצד
  - תיאור (בהיר/מעונן/וכו׳)
  - ☀ מקסימום היום · 🌙 מינימום הלילה
  - תחזית **מחר + 3 ימים** (מדלגים על "היום" כדי לא להתבלבל עם "כעת")
- **אייקונים:**
  - Sun: 32-spike sunburst בצהוב טהור (`#FACC15`/`#CA8A04`), גרדיאנט פנימי
  - Cloudsun: שמש קטנה (18 spikes) מאחורי ענן לבן
  - Rain: ענן אפור + 3 טיפות כחולות
  - Cloud: ענן לבן
- **טיפ:** קוד מזג אוויר 3 ("Overcast") הוקצה ל-`cloudsun` במקום `cloud` — בישראל גם בימים מעוננים יש שמש.

---

## 🌙 מצב לילה אוטומטי

בין **01:00 ל-05:00**, ה-`body` מקבל מחלקה `.night-mode` שמעמעמת את `#appRoot` ל-35% בהירות. **האזעקה (`#redAlertOverlay`)** אינה מועמעמת כי היא מתווספת ישירות ל-`body` ולא בתוך `#appRoot`.

---

## 🔑 קבועים שכדאי לדעת איפה לערוך

| מה | משתנה | קו |
|----|--------|-----|
| ערים פיקוד העורף | `MY_CITIES` | ~970 |
| מיקום מרחב מוגן | `SHELTER_INFO` | ~977 |
| תאריכי יום כיפור | `vaadYomKippurWindows` | ~720 |
| קואורדינטות מודיעין | `MODIIN` | ~880 |
| תקצירי פרשה | `PARSHIYOT` | ~460 |
| כללי הבית | `HOUSE_RULES` | ~270 |
| דמויות היסטוריות | `FIGURES` | ~320 |
| רבנים (לשבת) | `RABBIS` | ~620 |
| מוזיקה | `MUSIC` | ~720 |
| חדשות מקומיות | `LOCAL_NEWS` | ~700 |

---

## ⚠ דברים שלא לעשות

1. **אל תציע Firebase / React Native / מערכת אדמין מורכבת.** היה דיון ארוך, נדחה. ההצדקה: זה לבניין אחד עם 20 דירות. WhatsApp מטפל בהודעות.
2. **אל תשתמש ב-`lg:flex-[2]` של Tailwind Play CDN** — לא נתמך. השתמש ב-CSS מותאם.
3. **אל תשתמש ב-`-webkit-line-clamp` כפתרון יחיד** — בעייתי על RTL+Hebrew בסטרימר. השתמש בקיצוץ ידני ב-JS כגיבוי.
4. **אל תקליק אותי לעבור לעיצוב "ימים יוקרתי עם wood-and-gold"** — היה דיון, נדחה.
5. **אל תוסיף מקור הודעות חיצוני (Firestore וכו׳)** למרות שהיה דיון על זה — נדחה.
6. **אל תשנה את `index.html` בלי לאמת בפריוויו** — תמיד `preview_start` + `preview_eval` כדי לוודא שאין שגיאת syntax (במיוחד בגלל מרכאות בעברית — השתמש בגרשיים ״ במקום ").
7. **לא לעצור מוזיקה בשבת** — רק יום כיפור עוצר. **כן** מציגים "הגל השקט" אבל המוזיקה ממשיכה.

---

## 🛠 כלים שאני משתמש בהם בעבודה

- **Read / Edit / Write** לקבצים
- **Grep / Glob** לחיפוש
- **PowerShell** ל-Git operations + ffmpeg
- **Preview tools** (`preview_start`, `preview_eval`, `preview_resize`) לבדיקות
- **WebSearch / WebFetch** למחקר חיצוני (למשל למצוא תכני פרשה)
- **ffmpeg portable** (ב-`%TEMP%\ffmpeg_portable\`) לדחיסת MP3

---

## 🌐 בדיקות שאני עושה

לאחר כל שינוי משמעותי, אני:
1. **טוען בפריוויו** ברזולוציה 1280×720 (כמו הסטרימר)
2. **מודד גובה** של אלמנטים קריטיים — שאין גלישה
3. **מאלץ שבת** עם `window.vaadIsShabbatNow = function(){return true;}` ובודק את פאנל הגל השקט
4. **רואה אם יש שגיאות JS** ב-`window.vaadAlertStatus()`

---

## 💬 העדפות המשתמש

- **שפת תקשורת:** עברית
- **סגנון:** ישיר, ענייני, ברור. מעריך טבלאות והשוואות.
- **לפני שינויים גדולים:** מבקש לראות תצוגה מקדימה (preview file נפרד) לפני שאני נוגע ב-`index.html` הראשי.
- **רוצה אישור** לפני שאני בונה פיצ׳ר גדול שכרוך בכתיבת הרבה תוכן (כמו 54 תקצירי פרשה).
- **מעריך הצעות חלופיות** — מציע 2-3 אופציות, הוא בוחר.

---

## 📍 מצב נוכחי (Snapshot)

נכון לקומיט האחרון:

- ✅ פאנל מרכזי עם דמות יומית (חול) או הגל השקט + פרשה (שבת)
- ✅ 61 תקצירי פרשה הוטמעו
- ✅ מזג אוויר עם "כעת" + יום/לילה (☀/🌙) + 3 ימים קדימה
- ✅ כותרות נבחרות עם 3 כרטיסים + קיצוץ פרוגרסיבי
- ✅ כללי הבית עם 33 כללים + התאמת גופן דינמית
- ✅ מוזיקה מקומית autoplay
- ✅ הגל השקט עם exponential backoff + heartbeat
- ✅ מצב לילה 01-05
- ✅ רענון אוטומטי ב-04:00
- ✅ שעון מרכז גיאומטרי אמיתי של המסך
- ✅ קופירייט ח. כהן בכותרת
- ✅ כל הקבצים על GitHub Pages, סטרימר מציג חי

---

## 📞 שאלות לפני שמתחילים

אם המשתמש חוזר ופותח שיחה חדשה, שאל:
1. **מה הבעיה הספציפית?** הוא בדרך כלל יראה תמונה מהסטרימר.
2. **האם הסטרימר רענן את הדף?** Ctrl+Shift+R לעיתים פותר.
3. **האם זה קיים גם בפריוויו או רק בסטרימר?** הסטרימר רנדר Hebrew RTL טיפה אחרת.

---

*עדכון אחרון של מסמך זה: 2026-06-05*
